apply plugin: 'maven-publish'

// Путь к готовому AAR
def aarFile = file("$projectDir/libs/sdk-android.aar")

publishing {
    publications {
        release(MavenPublication) {
            groupId = 'com.migration.test'
            artifactId = 'migration-android-sdk'
            version = '0.0.9'
            artifact(aarFile)
        }
    }
}